<h2>ユーザー編集</h2>

<%= form_with model: @user, url: users_path, local: true, method: :patch do |f| %>

    <%= f.label :name, "name" %>
    <%= f.text_field :name %>

    <%= f.label :email, "email" %>
    <%= f.email_field :email %>

    <%= f.submit "編集" %>
<% end %>